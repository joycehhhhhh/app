import { router, Stack } from 'expo-router';
import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useRelationship } from '@/context/relationship-context';
export default function FutureEvents(){const {activeRelationship}=useRelationship();const events=[...activeRelationship.futureEvents].sort((a,b)=>a.date.localeCompare(b.date));return <ScrollView style={s.screen} contentContainerStyle={s.content} contentInsetAdjustmentBehavior="automatic"><Stack.Screen options={{title:'Future events',headerBackTitle:'',headerRight:()=> <Pressable accessibilityLabel="Add future event" onPress={()=>router.push('/add-future-event')} style={s.plusButton}><Text style={s.plus}>+</Text></Pressable>}}/>{events.length?events.map((event)=>{const days=Math.max(0,Math.ceil((new Date(`${event.date}T00:00:00`).getTime()-Date.now())/86400000));return <View key={event.id} style={s.card}><Text selectable style={s.days}>{days}</Text><Text selectable style={s.label}>DAYS TO GO</Text><Text selectable style={s.title}>{event.title}</Text><Text selectable style={s.date}>{new Date(`${event.date}T00:00:00`).toLocaleDateString(undefined,{month:'long',day:'numeric',year:'numeric'})}</Text><Text selectable style={s.note}>{event.note}</Text></View>}):<Text selectable style={s.empty}>Nothing planned yet. Use + to save something to look forward to.</Text>}</ScrollView>}
const s=StyleSheet.create({screen:{flex:1,backgroundColor:'#FBF7F1'},content:{padding:20,gap:14},card:{padding:22,gap:8,borderRadius:24,backgroundColor:'#F1E5DC'},days:{color:'#A26351',fontSize:48,fontWeight:'800',fontVariant:['tabular-nums']},label:{color:'#A17460',fontSize:10,fontWeight:'800',letterSpacing:1},title:{color:'#493B33',fontSize:25,fontWeight:'700'},date:{color:'#806F65',fontSize:14,fontWeight:'700'},note:{color:'#715F55',fontSize:15,lineHeight:22,marginTop:5},plusButton:{width:36,height:36,alignItems:'center',justifyContent:'center'},plus:{color:'#A0715C',fontSize:29,lineHeight:32},empty:{color:'#806F65',fontSize:16,lineHeight:22,paddingTop:16}});
