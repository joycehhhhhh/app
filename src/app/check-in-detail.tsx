import { Stack, useLocalSearchParams } from 'expo-router';
import { ScrollView, StyleSheet, Text } from 'react-native';
export default function CheckInDetail(){const {person='Brian'}=useLocalSearchParams<{person:string}>();return <ScrollView style={s.screen} contentContainerStyle={s.content} contentInsetAdjustmentBehavior="automatic"><Stack.Screen options={{title:`${person}'s check-in`}}/><Text selectable style={s.mood}>{person==='Brian'?'😔':'😊'}</Text><Text selectable style={s.title}>{person}’s day</Text><Text selectable style={s.note}>{person==='Brian'?'My manager made today harder than it needed to be. I’m ready for a quiet evening.':'I had a good day and am feeling grateful for our little routines.'}</Text></ScrollView>}const s=StyleSheet.create({screen:{flex:1,backgroundColor:'#FBF7F1'},content:{padding:20,gap:15},mood:{fontSize:48},title:{fontSize:27,fontWeight:'700',color:'#493B33'},note:{fontSize:16,lineHeight:24,color:'#715F55'}});
